{
  "name": "threshold",
  "version": "0.1.0",
  "private": true,
  "description": "No-code reasoning platform — dialogue produces knowledge graph, graph feeds reasoning",
  "scripts": {
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "cd client && npx vite",
    "dev:server": "cd server && npx nodemon index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
